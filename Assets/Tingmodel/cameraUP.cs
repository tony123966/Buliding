using UnityEngine;
using System.Collections;

public class cameraUP : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	




	}


    void OnGUI()
    {
        /*
      
        if (Input.GetKeyDown(KeyCode.A))
        {
            GameObject bb =  GameObject.Find("build");
            int bbcc = bb.transform.childCount;

            print(bbcc);
            for (int i =1;i<bbcc ;i++)
            {
                bb.transform.GetChild(i).GetChild(1).GetComponent<UIcontrol>().numberslider.value++;

                bb.transform.GetChild(i).GetChild(1).GetComponent<UIcontrol>().setnumberslidervalue();


            }



        }
        if (Input.GetKeyDown(KeyCode.S))
        {
            GameObject bb = GameObject.Find("build");
            int bbcc = bb.transform.childCount;

            for (int i = 1; i < bbcc; i++)
            {
                bb.transform.GetChild(i).GetChild(1).GetComponent<UIcontrol>().numberslider.value--;

                bb.transform.GetChild(i).GetChild(1).GetComponent<UIcontrol>().setnumberslidervalue();


            }
        }

        */
    }

}
